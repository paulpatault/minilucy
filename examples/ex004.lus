node counting (tick, top: bool) returns (o: int);
var v: int;
let
    v = if top then 1 else 0;
    o = if tick then v else 0 -> pre o + v;
tel

node check(x: bool) returns (y: bool);
let
  y = counting(x, x) < 2;
tel
