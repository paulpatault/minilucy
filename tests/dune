; (rule
;  (deps
;   (:neg (glob_files_rec negatifs/*.lus)))
;  (action
;    (with-stdout-to tests.output (run ../minilucy.exe %{neg} main0 -v))))

; (rule
;  (alias runtest)
;  (action (diff tests.expected tests.output)))
