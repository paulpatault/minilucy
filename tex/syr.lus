node syracuse (i: int)
returns (o: int);
let
  automaton
  | Even ->
      o = i -> pre o / 2;
      until o mod 2 = 1
      continue Odd
  | Odd ->
      o = i -> pre o * 3 + 1;
      until o mod 2 = 0
      continue Even
  end
tel
